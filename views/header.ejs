<nav>

  
    <div class="nav">

        <% if(locals.user && !locals.user.isAdmin){ %>
            <a class="nav-link text-light" href="/users/logout">Log Out</a>
            <a class="nav-link text-light" href="#"><%= user.name %></a>
        <% }else if(locals.user && locals.user.isAdmin){ %>
            <a class style="color:rgb(28, 28, 204);text-align:left,"  href="/admin/viewEmployees">seeEmployees</a>
           <span> </span> <a class style="color:rgba(250, 14, 14, 0.247);text-align:right ,"  href="/users/sinup">Add user</a>
            <a class style="color:rgb(5, 218, 51);text-align:center ," " href="/users/logout">Log Out</a>
        <% }else{ %>
            <a class="nav-link text-light" href="/users/login">Log In</a>
            <a class="nav-link text-light" href="/users/sinup">Register</a>
        <% } %>  

    </div>
    
  </nav>